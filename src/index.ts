export class InstantTable {
  public search (searchString: string): InstantTable {
    // do something
    return this
  }

  public sort (sortString: string, columnName: string): void {
    // do something
  }

  public paginate (pageNumber: number, pageLimit: number): void {
    // do something
  }

  private __setMetaData (): void {
    // do something
  }
}
